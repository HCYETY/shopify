<style>
    #shopify-section-{{section.id}}  .button-ATC{
        background: #14a9de;
        border: none;
  }
   #shopify-section-{{section.id}} .little-collection {
  width: 100%;
  padding: 4.69vw 0;
  background: linear-gradient(0deg, #F2FBFF, #A3D6EF);
}
  #shopify-section-{{section.id}} .little-collection h2 {
  color: #333;
  text-align: center;
  font-size: 1.56vw;
  font-weight:bold;
  margin-bottom: 2.34vw;
}
  #shopify-section-{{section.id}} .little-collection .wrapper {
  width: 58.59vw;
  margin: 0 auto;
  display: flex;
  flex-direction: row;
  gap: 0.59vw;
  margin-top: 2.34vw;
}
  #shopify-section-{{section.id}} .little-collection .product-card {
    width: calc(25% - 0.78vw);
    position:relative;
}
  #shopify-section-{{section.id}}  .sale-count {
      width: fit-content;
      position: absolute;
      padding: 0.39vw;
      padding-right:1vw;
      border-radius: 0.31vw;
      top: 0.4vw;
      overflow: hidden;
      left: 0vw;
      z-index:2;
      font-size: 0.625vw;
      color: #fff;
      background: #F8611F;
      clip-path: polygon(100% 0, 87% 47%, 100% 100%, 0 100%, 0 0);
    }
   
  #shopify-section-{{section.id}} .little-collection .product-card .card {
  width: 100%;
  height: 21.48vw;
  background-color: #fff;
  border-radius: 1.17vw;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  align-items: center;
}
  #shopify-section-{{section.id}} .little-collection .product-card .card .top {
  padding: 0 1.95vw;
  height: 60%;
  width: 100%;
  background-color: #fff;
  display: flex;
  justify-content: center;
  align-items: center;
}
  #shopify-section-{{section.id}} .little-collection .product-card .card .top img {
  width: 100%;
  aspect-ratio: 1;
  object-fit: contain;
}
  #shopify-section-{{section.id}} .little-collection .product-card .card .bottom {
  height: 40%;
  width: 100%;
  padding-top: 0.78vw;
  background: #fff;
  padding: 0.39vw 0.78vw;
}
   #shopify-section-{{section.id}} .little-collection .product-card .card .bottom .btns {
     margin-top:0.625vw;
   }
  #shopify-section-{{section.id}} .little-collection .product-card .card .bottom .name {
  font-size: 0.94vw;
  font-weight: bold;
      display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
  #shopify-section-{{section.id}} .little-collection .product-card .card .bottom .price {
  display: flex;
  justify-content: center;
  margin-left: auto;
  align-items: end;
  margin-top: 0.78vw;
  display: flex;
  flex-direction: row;
  flex-wrap: nowrap;
}
  #shopify-section-{{section.id}} .little-collection .product-card .card .bottom .price .new-price {
  font-size: 1.25vw;
  color: #14A9DE;
}
  #shopify-section-{{section.id}} .little-collection .product-card .card .bottom .price .old-price {
  font-size: 0.78vw;
  color: #666;
}
  #shopify-section-{{section.id}} .little-collection .product-card .card .bottom .info-desc {
  font-size: 0.78vw;
  color: #666;
  margin: 0.55vw 0;
}
 #shopify-section-{{section.id}} .button-ATC,#shopify-section-{{section.id}} .card-action .button {
  text-transform: uppercase;
  width: 10.94vw;
  height: 2.34vw;
  border-radius: 999px;
  font-size: 0.78vw;
  display: flex;
  justify-content: center;
  align-items: center;
  cursor: pointer;
  background: #14A9DE;
  color: #fff;
  border: none;
}
  #shopify-section-{{section.id}} .little-collection .product-card .desc {
  color: #666666;
  text-align: center;
  margin-top: 0.78vw;
  font-size: 0.7vw;
}
   #shopify-section-{{section.id}} .swiper-button-prev:after,#shopify-section-{{section.id}} .swiper-button-next:after{
    color:#fff;
    font-size: 1.25vw;
 }
  #shopify-section-{{section.id}} .swiper-button-prev,#shopify-section-{{section.id}} .swiper-button-next{
    background:#14A9DE;
    border-radius:999px;
    width:2.25vw;
    height:2.25vw;
 }
  @media(max-width:750px){
 #shopify-section-{{section.id}} .little-collection {
    width: 100%;
    padding: 16vw 0;
    background: linear-gradient(0deg, #F2FBFF, #A3D6EF);
  }
   #shopify-section-{{section.id}} .little-collection h2 {
    color: #333;
    font-size: 4.8vw;
    text-align: center;
    margin-bottom: 4.8vw;
  }
   #shopify-section-{{section.id}} .little-collection .wrapper {
    width: 100%;
    margin: 0 auto;
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 2vw;
    row-gap: 4vw;
    margin-top: 8vw;
    padding: 0 6.67vw;
  }
   #shopify-section-{{section.id}} .little-collection .wrapper::-webkit-scrollbar {
    display: none;
  }
   #shopify-section-{{section.id}} .little-collection .product-card {
    width: calc(50% - 1.07vw);
  }
   #shopify-section-{{section.id}} .little-collection .product-card .card {
    width: 100%;
    height: 73.33vw;
    background-color: #fff;
    border-radius: 4vw;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    align-items: center;
  }
   #shopify-section-{{section.id}} .little-collection .product-card .card .top {
    padding: 0 6.67vw;
    height: 60%;
    width: 100%;
    background-color: #fff;
    display: flex;
    justify-content: center;
    align-items: center;
  }
   #shopify-section-{{section.id}} .little-collection .product-card .card .top img {
    width: 80%;
    aspect-ratio: 1;
    object-fit: contain;
  }
   #shopify-section-{{section.id}} .little-collection .product-card .card .bottom {
    height: 40%;
    width: 100%;
    padding-top: 2.67vw;
    background: #fff;
    padding: 1.33vw 2.67vw;
  }
   #shopify-section-{{section.id}} .little-collection .product-card .card .bottom .name {
    font-size: 3.2vw;
    font-weight: bold;
       display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  }
   #shopify-section-{{section.id}} .little-collection .product-card .card .bottom .price {
    display: flex;
    justify-content: center;
    margin-left: auto;
    align-items: end;
    margin-top: 2.67vw;
  }
   #shopify-section-{{section.id}} .little-collection .product-card .card .bottom .price .new-price {
    font-size: 4.07vw;
    color: #14A9DE;
  }
   #shopify-section-{{section.id}} .little-collection .product-card .card .bottom .price .old-price {
    font-size: 2.67vw;
    color: #666;
  }
   #shopify-section-{{section.id}} .little-collection .product-card .card .bottom .info-desc {
    font-size: 2.67vw;
    color: #666;
    margin: 1.87vw 0;
  }
   #shopify-section-{{section.id}} .little-collection .product-card .card .bottom .btns {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 2.67vw;
    gap: 3.2vw;
  }
 #shopify-section-{{section.id}}  .button-ATC , #shopify-section-{{section.id}} .card-action .button  {
    text-transform: uppercase;
    width:auto;
    max-width:unset;
    height: 8vw;
    border-radius: 999px;
    font-size: 2.67vw;
    display: flex;
    justify-content: center;
    align-items: center;
    cursor: pointer;
    background: #14A9DE;
    color: #fff;
  }
    #shopify-section-{{section.id}}  .button-ATC{
        background: #14a9de;
        border: none;
  }
   #shopify-section-{{section.id}} .little-collection .product-card .desc {
    color: #666666;
    text-align: center;
    margin-top: 2.67vw;
    font-size: 2.4vw;
  }
   #shopify-section-{{section.id}}  .sale-count {
    width: fit-content;
    position: absolute;
    padding: 1.2vw;
    padding-right:1.8vw;
    border-radius: 0.82vw;
    top: 3vw;
    overflow: hidden;
    left: 0vw;
    z-index:2;
    font-size: 2.25vw;
    color: #fff;
    background: #F8611F;
    clip-path: polygon(100% 0, 87% 47%, 100% 100%, 0 100%, 0 0);
  }

  /* Add carousel styles */
  #shopify-section-{{section.id}} .swiper-container {
    position: relative;
    padding: 0;
  }
  
  #shopify-section-{{section.id}} .swiper-button-next,
  #shopify-section-{{section.id}} .swiper-button-prev {
    color: #14A9DE;
    width: 2.5vw;
    height: 2.5vw;
  }
  
  #shopify-section-{{section.id}} .swiper-pagination-bullet-active {
    background: #14A9DE;
  }
  
  #shopify-section-{{section.id}} .swiper-pagination {
    position: relative;
    margin-top: 1.5vw;
  }
  
  @media(max-width:750px) {
    #shopify-section-{{section.id}} .swiper-button-next,
    #shopify-section-{{section.id}} .swiper-button-prev {
      width: 8vw;
      height: 8vw;
    }
    
    #shopify-section-{{section.id}} .swiper-pagination {
      margin-top: 5vw;
      display:none;
    }
  #shopify-section-{{section.id}} .swiper-button-prev:after,#shopify-section-{{section.id}} .swiper-button-next:after{
    color:#fff;
    font-size: 3.25vw;
 }
  }
</style>

<div class="little-collection" data-id="{{ section.settings.bind }}">
  <h2>{{ section.settings.title }}</h2>
  <div class="wrapper swiper-container">
    <div class="swiper-wrapper">
      {% if section.settings.collection != blank %}
        {% assign collection = collections[section.settings.collection] %}
        {% for product in collection.products %}
          <div class="product-card swiper-slide">
            {% assign discount = product.compare_at_price | minus: product.price %}
            {% assign discount_percentage = discount | times: 100 | divided_by: product.compare_at_price | round %}
            {% if product.compare_at_price > product.price %}
              <div class="sale-count">
                Save {{ discount_percentage }}%
              </div>
            {% endif %}
            <div class="card">
              <div class="top">
                <img src="{{ product.featured_image | img_url: 'master' }}" alt="{{ product.title | escape }}">
              </div>
              <div class="bottom">
                <div class="price">
                  <div class="new-price">
                    {{ product.price | money }}
                  </div>
                  {% if product.compare_at_price > product.price %}
                    <s class="old-price">
                      {{ product.compare_at_price | money }}
                    </s>
                  {% endif %}
                </div>
                <div class="btns">
                    <div class="card-action has-compare{% if settings.show_product_marquee %} has-marquee{% endif %}">
                        {% render 'custom-add-to-cart', product: product, id: 'form', position: position, sectionId: sectionId %}
                    </div>
                </div>
              </div>
            </div>
            <div class="desc">
              {{ product.title }}
            </div>
          </div>
        {% endfor %}
      {% endif %}
    </div>
    <div class="swiper-pagination"></div>
    <div class="swiper-button-prev"></div>
    <div class="swiper-button-next"></div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    new Swiper(' #shopify-section-{{section.id}} .swiper-container', {
      slidesPerView: 4,
      spaceBetween: 0,
      loop: true,
       autoplay: {
        delay: 5000,
        disableOnInteraction: false, 
      },
       navigation: {
        nextEl: '#shopify-section-{{section.id}} .swiper-button-next',
        prevEl: '#shopify-section-{{section.id}} .swiper-button-prev',
      },
      breakpoints: {
        320: {
          slidesPerView: 1.4,
          spaceBetween: 20
        },
        480: {
          slidesPerView: 1.4,
          spaceBetween: 20
        },
     
        1024: {
          slidesPerView: 4,
          spaceBetween: 10
        }
      }
    });
  });
</script>


{% schema %}
{
  "name": "Collection Carousel",
  "tag": "section",
  "class": "product-collection-section",
  "settings": [
    {
      "type": "text",
      "id": "bind",
      "label": "bind"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Section Title",
      "default": "Materials for All Laser Engravers"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    }
 
  ],
  "presets": [
    {
      "name": "Collection Carousel"
    }
  ]
}
{% endschema %}